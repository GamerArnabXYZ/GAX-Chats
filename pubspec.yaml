name: gax_chats
description: GAX Chats - Real-time messaging app
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: 3.6.0
  firebase_auth: 5.3.1
  firebase_database: 11.1.4
  intl: ^0.19.0
  url_launcher: ^6.3.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
