name: gax_chats
description: Production Ready Real-time Chat App
version: 1.0.0+1
environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.15.0
  firebase_auth: ^4.7.3
  firebase_database: ^10.2.5
  intl: ^0.18.1

flutter:
  uses-material-design: true
